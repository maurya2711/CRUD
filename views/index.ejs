<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUD</title>
    <link rel="stylesheet" href="/index.css" />
  </head>
  <body>
    <h1>My First CRUD Page using node js, mongodb and express</h1>
    <div class="container">
      <div class="create">
        <h4>Write any Quotes with name to contribute</h4>
        <form action="/products/create" method="POST">
          <input type="text" placeholder="name" name="name" />
          <input type="text" placeholder="quote" name="quote" />
          <button type="submit">Write</button>
        </form>
      </div>

      <div class="update">
        <h3>UPDATE Quotes</h3>
        <p>Enter the name and your quote you want to update.</p>
        <input id="updateName" type="text" placeholder="Name" />
        <input id="update-quote" type="text" placeholder=" Write Quote" />
        <button id="update-button">UPDATE</button>
      </div>
      <div class="delete">
        <h3>DELETE Quotes</h3>
        <p>Enter the name of person of quote you want to Delete</p>
        <input id="input" type="text" placeholder="name" />
        <button id="delete-button">Delete</button>
      </div>
      <div class="read">
        <h2>Quotes:=></h2>
        <table class="table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Quote</th>
            </tr>
          </thead>
          <tbody>
            <% for(var i = 0; i < quotes.length; i++) { %>
            <tr>
              <td><%= quotes[i].name %></td>
              <td><%= quotes[i].quote %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
    <script src="/main.js"></script>
  </body>
</html>
